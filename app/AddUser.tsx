import React from 'react';
import { View, Text, TextInput, Button } from 'react-native';

const AddUser = () => {
    return (
        <View>
            <Text>Add User</Text>
        </View>
    );
};

export default AddUser;